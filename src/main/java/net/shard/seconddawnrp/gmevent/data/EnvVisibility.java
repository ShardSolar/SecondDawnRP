package net.shard.seconddawnrp.gmevent.data;

public enum EnvVisibility {
    /** Block glows — players can see the source. */
    VISIBLE,
    /** No visual indicator — players only feel the effect. */
    HIDDEN
}